Navigacioni meni:

Table: Navigation_Items

id (int, primary key)
name (varchar)
url (varchar)
parent_id (int, foreign key to Navigation_Items.id, nullable)

----------

Korisnici:

Table: users

id (primary key)
username
password
email
fullname
phone_number
role_id (foreign key references roles table)
country_id (foreign key references countries table)
gender_id (foreign key references genders table)

Table: roles

id (primary key)
name

Table: countries

id (primary key)
name

Table: genders

id (primary key)
name

----------

Narucivanje:

orders

order_id (primary key)
user_id (foreign key to users table)
fullname
email
phone
country_id (foreign key to countries table)
address
postcode
created_at
total_price

Order Items Table

order_item_id (primary key)
order_id (foreign key to Order table)
product_id (foreign key to Product table)
quantity
item_price

----------

Proizvodi:

Table name: products

id (int, primary key, auto-increment)
name (varchar)
description (text)
price (decimal)
image (varchar)
rating_id
category_id (int, foreign key to categories table)
brand_id (int, foreign key to brands table)
gender_id (int, foreign key to genders table)

Product_Image:

image_id (primary key)
product_id (foreign key to Product table)
image_url
caption caption for the image (optional)

Table Name: ratings

id (int)	
user_id	(int)	
product_id	(int)
rating	(int)
created_at	(timestamp)

Table name: product_colors

id (int, primary key, auto-increment)
color (varchar)

Table name: product_sizes

id (int, primary key, auto-increment)
size (varchar)

Table name: categories

id (int, primary key, auto-increment)
name (varchar)

Table name: brands

id (int, primary key, auto-increment)
name (varchar)

Table name: genders

id (int, primary key, auto-increment)
name (varchar)

----------

Anketa:

Poll Table

id (primary key)
question
category_id (foreign key to Category table)
is_active (boolean)
created_at
updated_at

Poll Option Table

id (primary key)
poll_id (foreign key to Poll table)
option
created_at
updated_at

Poll Answer Table

id (primary key)
user_id (foreign key to User table)
poll_id (foreign key to Poll table)
poll_option_id (foreign key to Poll Option table)
created_at
updated_at

----------

OD CHAT GPT

Users:

user_id (primary key)
username
password
email
role_id (foreign key to Roles table)
gender_id (foreign key to Gender table)
phone_id (foreign key to Phone table)
created_at
updated_at

Roles:

role_id (primary key)
role_name

Gender:

gender_id (primary key)
gender_name

Phone:

phone_id (primary key)
phone_number
country_id (foreign key to Country table)

Country:

country_id (primary key)
country_name

Order:

order_id (primary key)
user_id (foreign key to Users table)
full_name
email
phone_id (foreign key to Phone table)
address
postcode
created_at
updated_at

Product:

product_id (primary key)
product_name
product_description
price
category_id (foreign key to Category table)
brand_id (foreign key to Brand table)
gender_id (foreign key to Gender table)

Product_Image:

image_id (primary key)
product_id (foreign key to Product table)
image_url

Color:

color_id (primary key)
color_name

Product_Color:

id (primary key)
product_id (foreign key to Product table)
color_id (foreign key to Color table)

Size:

size_id (primary key)
size_name

Product_Size:

id (primary key)
product_id (foreign key to Product table)
size_id (foreign key to Size table)

Brand:

brand_id (primary key)
brand_name

Category:

category_id (primary key)
category_name

Rating:

rating_id (primary key)
user_id (foreign key to Users table)
product_id (foreign key to Product table)
rating_value
created_at
updated_at

Poll:

poll_id (primary key)
user_id (foreign key to Users table)
category_id (foreign key to Category table)
answer